# Install torch for your platform/CUDA build first if you need a specific one:
# https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15
numpy>=1.24,<2  
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
pillow>=9.0
pyarrow>=12.0
h5py>=3.8
huggingface_hub>=0.20
requests>=2.28
neuraloperator  # pde/ --score-arch fno (neuralop.models.FNO)
